# ---- 核心运行依赖 ----
numpy>=1.24
pandas>=2.0
pyarrow>=14.0
pydantic>=2.5
pydantic-settings>=2.1
PyYAML>=6.0
python-dateutil>=2.8

# ---- API ----
fastapi>=0.110
uvicorn[standard]>=0.27

# ---- 数据源（按需，未安装时自动降级为 mock）----
akshare>=1.12
# tushare>=1.4

# ---- 存储（可选）----
# sqlalchemy>=2.0
# psycopg[binary]>=3.1
# duckdb>=0.10
# redis>=5.0

# ---- 模型（P2 起启用）----
# lightgbm>=4.0
# xgboost>=2.0
# scikit-learn>=1.3
# torch>=2.1

# ---- 因子验证 / 绩效（可选）----
# alphalens-reloaded>=0.4
# quantstats>=0.0.62

# ---- 任务调度 / 实验管理（P6）----
# prefect>=2.14
# mlflow>=2.10
